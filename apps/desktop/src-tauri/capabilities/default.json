{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. The engine sidecar is spawned from Rust via tokio::process (engine_bridge.rs), not via plugin-shell webview IPC, so no shell:allow-spawn grant is needed here — capabilities only gate webview->plugin calls, and this app has no plugin-shell usage to gate.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
