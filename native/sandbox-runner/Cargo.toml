[package]
name = "openfusion-sandbox"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"
description = "Fail-closed native process containment runner for OpenFusion"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

